<?xml version="1.0" encoding="utf-8"?>
<resources>
  <!-- Android 12+: hide centered launcher icon on the system splash by using a same-color drawable. -->
  <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
    <!-- Required: v31 overlay replaced the base style and dropped windowBackground, leaving a black window on some devices until the WebView paints. -->
    <item name="android:windowBackground">@color/app_window_bg</item>
    <item name="android:colorBackground">@color/app_window_bg</item>
    <item name="android:windowSplashScreenBackground">@color/splash_background</item>
    <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_invisible</item>
    <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
    <item name="android:windowSplashScreenAnimationDuration">0</item>
  </style>
</resources>
